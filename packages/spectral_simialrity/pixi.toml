[project]
name = "spectral_similarity"
version = "0.1.0"
description = "A rust package for spectral similarity"
authors = []
channels = ["conda-forge"]
platforms = ["linux-64"]

[tasks]
build = "rattler-build build --recipe recipe/recipe.yaml"

[dependencies]
rattler-build = ">=0.1.0"
rust = ">=1.70"
compilers = "*"
cargo-bundle-licenses = "*"
maturin = ">=1.0"
