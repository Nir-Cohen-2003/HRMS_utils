[project]
name = "mass_decomposition"
version = "0.1.0"
license = "Apache-2.0"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "mass_decomposition._internal"

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"
