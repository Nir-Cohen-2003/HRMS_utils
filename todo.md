1. in mass decomposition, for sodium the "subtract the addcut mass then decompose the neutralized masses" doesn't work, since it can easily lose the sodium atom and have a proton instead
2. in similarity, add precursor mz argument, and ignore all fragmnets above it and in the 1. Da range below it too. also enable, as a boolen keyword arguemnt, to ignore the precursor-  ignore_precursor. find the precursor in the spectrum based using the supplied mass tolerance.
3. in mass decomposition, allow for water absorption (again, as an optional parameter)
4. add more fucntion to the spectral similarity, in addition to entropy. the fucntion: a general cosine similarity, named "general_cosine_similarity", with arguments for numerical values for the power given to the inteisty (e.g. 0.5 if we want to take the square rrot of the intesity) and mass (eg 1 if we want to give greater importance to the heavier masses.), and a special one with the parameters given where the power on intensity is 0.5, and on mass is 2, named mass_weighted_cosine_similarity. also make a fucntion named "explained_intesity" which is cosine simialrity with power of 0 on mass (soi no wight on it) and power of 0.5 on intensity, and a function name "mass_weighted_explained_intesity, where mass has weight 1 and intensity 0.5. make those all have python dicstring ecplaining them, and add them to the name space of hte polatrs plugin